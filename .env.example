# tmex 环境配置示例
# 复制为 .env 并根据实际情况修改

# ==================== 核心配置 ====================

# 主密钥（32 bytes base64），用于加密敏感数据
# 生产环境必须设置，否则拒绝启动
# 生成方式: head -c 32 /dev/urandom | base64
TMEX_MASTER_KEY=tGd9gPmdUkJrpRQK+db60sc+NkxymxgGqKrReDU4Kus=

# 服务基础 URL（用于生成 Webhook 回调地址等）
TMEX_BASE_URL=http://127.0.0.1:8085

# 站点名称（默认值，实际可在设置页修改并持久化）
TMEX_SITE_NAME=tmex

# ==================== Gateway ====================

# Gateway 服务端口
GATEWAY_PORT=9663

# SQLite 数据库路径
DATABASE_URL=/tmp/tmex.db

# 设置默认值（实际可在设置页修改并持久化）
TMEX_BELL_THROTTLE_SECONDS=6
TMEX_SSH_RECONNECT_MAX_RETRIES=2
TMEX_SSH_RECONNECT_DELAY_SECONDS=10

# ==================== 前端配置 ====================

# Gateway 访问地址（前端代理目标，默认为 http://localhost:9663）
TMEX_GATEWAY_URL=http://localhost:9663

# 前端服务端口（vite dev 使用，默认为 9883）
FE_PORT=9883

# ==================== 开发配置 ====================

# 开发模式（跳过部分安全检查）
NODE_ENV=development
