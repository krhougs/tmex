# Prompt Archive

时间：2026-02-12

## 用户原始需求

- 「请为前端新增 manifest 用来安装 PWA，PWA 的名称为数据库中设置的名字，fe 的 dev 模式和最终打包的 app 中都需要生效」
- 「Implement the plan.」

## 实施约束（从仓库规范提取）

- 先存档再干活。
- 使用简体中文沟通。
- 不猜测 API，优先基于现有代码结构做最小改动。
- 需要同时覆盖 `apps/fe` 开发模式与 `packages/app` 打包运行模式。

