# Prompt Archive

## 用户需求

通过本地 ssh agent 登陆还是不行。目的不是做一个测试本身，而是通过测试使 ssh 连接至远端 tmux 功能工作。

另外发现现在缺少必要的 ssh 相关日志，前端无论什么原因连接失败都会提示验证失败，需要修复整个 ssh 连接 tmux 功能。

## 用户偏好（本轮已确认）

- `sshConfigRef` 本次先不实现完整解析，需显式报未实现，避免误导。
- 前端错误展示优先直接显示后端原始错误（rawMessage）。

