# 原始 prompt 存档

## 初始任务

以main为base创建新的worktree排查问题：
当前终端渲染有点小问题：
1. 似乎没有enforce行高，导致不同平台上文字渲染行高高度不一致
2. 字符渲染在垂直方向被截断，表现出来就是f y g这种预期位置超出字符背景block的部分会被掐头去尾

两个问题是独立问题，请勿混淆。
请找出独立根因而不是为了看起来好了强行修改渲染参数。
这个问题需要我手动验收。

## 追加补充

另外我补充一下：你的修复没有问题，但是我们需要允许文字渲染超出其字形盒以兼容奇怪的Unicode
