# Prompt Archive

日期：2026-02-13

## 上下文
- 项目运行时：Bun.js（代码不兼容 Node.js）。
- 前端已完成一次“shadcn/ui + Base UI”重构并合并到当前分支（主工作区 HEAD: `4a09e2a feat(fe): rebuild frontend (shadcn + base-ui)`）。
- 现阶段用户反馈：功能不全、UI 太丑，需要继续修复并全局使用 Nova 主题和配色，忽略所有旧 UI 元素。

## 用户要求（摘录）
- 完全使用 shadcn/ui + Base UI 实现前端，使用 shadcn 默认控件主题并支持全局 dark mode。
- 删除现有所有前端 e2e 测试并为新的前端重新编写完整、系统性的测试。
- 充分考虑不同平台行为，尤其 iOS PWA 安全区。
- 保证新 codebase 覆盖现有所有业务和功能。
- 设计风格按 2026 国际工具产品最佳实践，布局紧凑。
- 妥善处理移动端与触摸屏 UI 边界情况。
- 需要搜索查看 shadcn/ui 最新文档；Base UI 是新版本标配；永远记得上网搜索最新文档。

## 最新反馈
- “功能不全 继续修复 ui太丑 应该全局使用nova主题和配色 无视所有旧的ui元素”。

## 后续反馈（移动端/PC 可用性）
- 1. 手机上 sidebar 会显示一个无意义的竖条（右侧边缘，像 padding）。
- 2. 快捷键栏如果手放在按钮上根本无法触发。
- 3. xterm 字体可以小一点点。
- 4. 手机上触发键盘后快捷键点按不了；正确行为是键盘弹出后快捷键仍可点按，且不会触发键盘收起。
- 5. 设置界面根本没法用（点不了/遮挡/结构差三项同时存在）。
- 6. 编辑器发送按钮不工作（能点但无反应）。
- 7. PC 端终端区域没有撑满屏幕（右侧留白 + xterm 内部未铺满）。
- 8. iOS 底部安全区颜色应该和终端背景一样。
- 9. sidebar 应该盖住安全区空白而不是被挡住/留白。

## 用户指令
- 合并两个修复 plan（移动端交互/安全区）为一个统一 plan。
- 归档该 plan，先不执行。
