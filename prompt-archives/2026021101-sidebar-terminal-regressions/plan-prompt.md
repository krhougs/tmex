# Prompt Archive

## 2026-02-11 用户原始问题

现在依然有问题：
1. sidebar中缺少关闭pane或者window的按钮（当一个window中所有pane被关闭的时候，应该关闭window）
2. 新window的按钮在高亮状态看不清
3. 所有页面的宽度高度，两个方向的状态同步是坏的，所有页面一打开里面的内容排版都是乱的
4. 之前清楚重复的bug的时候带来了新bug，TUI中很多输入都是一闪而过

## 2026-02-11 实施指令

Implement the plan.

## 关键约束与确认

- 关闭后 URL 失效时：不自动跳转，由页面提示失效状态。
- Sidebar 关闭按钮显示策略：始终显示。
- 尺寸同步策略：改为客户端尺寸同步（不再使用 `resize-pane -x/-y` 去硬改 pane 尺寸）。
- 问题 3 的主症状：终端列行/布局错乱，而非纯 CSS 容器错位。
